<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Nome</th>
      <th scope="col">Título</th>
      <th scope="col">Endereço</th>
      <th scope="col">Data</th>
      <th scope="col">Ações</th>
    </tr>
  </thead>
  <tbody>
    <% @exams.each do |exam| %>
      <tr>
        <th scope="row"><%= exam.id %></th>
        <td><%= exam.pacient %></td>
        <td><%= exam.title %></td>
        <td><%= exam.address %></td>
        <td><%= exam.date.strftime("%d/%m/%Y as %H:%M") %></td>
        <td>
          <div class="btn-group">
            <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="fas fa-bars i-white"></i>
            </button>
            <div class="dropdown-menu">
              <%= link_to "Visualizar", exam_path(exam), class: "dropdown-item" %>
              <%= link_to "Editar", edit_exam_path(exam), class: "dropdown-item" %>
              <%= link_to "Apagar", exam_path(exam), method: :delete, data: { confirm: "Confirma a exclusão ?" }, class: "dropdown-item" %>
            </div>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
